{
  "name": "college-management-system",
  "version": "1.0.0",
  "description": "Full-stack College Management System",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && cross-env PORT=5001 npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "db:setup": "cd backend && npm run db:generate && npm run db:push",
    "db:studio": "cd backend && npm run db:studio",
    "clean": "rm -rf backend/dist frontend/build",
    "clean:install": "npm run clean && npm run install:all"
    "postinstall": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "keywords": ["college", "management", "education", "fullstack"],
  "author": "Your Name",
  "license": "MIT"
}
